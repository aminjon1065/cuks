# Files that run inside Linux containers must keep LF on every platform — a CRLF
# shebang (`#!/usr/bin/env bash\r`) or Dockerfile makes the container fail to start.
# Without this, a Windows checkout (core.autocrlf=true) would corrupt them.
*.sh text eol=lf
Dockerfile text eol=lf
*.Dockerfile text eol=lf
